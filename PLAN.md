# Promethena — 제품 사이트 계획

내가 만든 소프트웨어 제품들을 모아 보여주고 파는 사이트.
`Products/` 폴더에 md 파일이 늘어나면 사이트에 자동으로 반영되는 것이 최우선 요구사항이다.

---

## 목표

1. **md 추가 = 사이트 반영.** 제품을 하나 더 만들었을 때 md 파일 하나만 쓰면 끝나야 한다. 라우팅·목록·링크 코드를 손대는 일이 없어야 한다.
2. **미완성 제품도 정직하게 올린다.** 지금 제품들은 프로토타입 단계다. "완성된 것만 올린다"로 가면 사이트가 영원히 비어 있다. 상태를 배지로 드러내고 그 상태에 맞는 행동을 유도한다.
3. **한국어·영어 둘 다.** 제품명은 영문, 본문은 한국어로 쓰고 있다. 영어판은 나중에 채워도 사이트가 깨지지 않아야 한다.
4. **소소한 용돈벌이 규모에 맞는 운영 비용.** 서버 없음, DB 없음, 월 고정비 0원. 정적 사이트로 끝낸다.

---

## 파일 규약

### 파일명

```
Products/{slug}.{lang}.md
```

```
Products/
  Apollo.ko.md
  Apollo.en.md
  Hestia.ko.md
  Hestia.en.md
```

`slug`이 URL이 되고 `lang`이 언어 라우팅이 된다. 한쪽 언어 파일이 없으면 그 언어 목록에서 자동으로 제외된다 — 영어판을 천천히 채워도 된다.

### 프론트매터

```yaml
---
name: Apollo
tagline: 공유 세계관 소설 플랫폼
status: prototype        # idea | prototype | beta | live
price: 0                 # optional. status가 live일 때만 의미 있음
tags: [웹, 창작]
demo: https://...        # optional. 없으면 데모 버튼 숨김
---
```

`name`, `tagline`, `status`, `tags`는 필수. 빠지면 **빌드가 실패한다** — 조용히 깨진 카드가 배포되는 일을 막는다.

**본문 형식은 통일하지 않는다.** 현재 Apollo.md와 Hestia.md는 구조가 서로 다르다(Hestia에는 기술 스택 표와 "현재 상태" 절이 있고 Apollo에는 없다). 본문 틀을 강제하면 제품마다 글 쓰는 게 괴로워지고, 결국 md를 안 쓰게 된다. 카드와 필터에 필요한 값만 프론트매터로 빼고 본문은 자유롭게 둔다.

---

## 사이트 구조

| 경로 | 내용 |
|---|---|
| `/` | 한국어 홈 — 제품 카드 그리드 |
| `/p/{slug}` | 한국어 상세 — md 본문 렌더 + CTA |
| `/en/` | 영어 홈 |
| `/en/p/{slug}` | 영어 상세 |

페이지 템플릿 2개(목록, 상세)로 전부 처리한다. 제품이 몇 개가 되어도 템플릿은 그대로다.

---

## status가 화면을 결정한다

| status | 카드 배지 | CTA |
|---|---|---|
| `idea` | 회색 · 구상 중 | 없음 |
| `prototype` | 호박색 · 프로토타입 | 대기자 등록 |
| `beta` | 시안색 · 베타 | 대기자 등록 + 데모 보기 |
| `live` | 시안색 · 가격 표시 | 구매 |

판매와 포트폴리오를 한 사이트에서 겸한다. `live` 제품은 구매를 받고, 그 전 단계는 관심을 받는다.

CTA 분기는 컴포넌트 한 곳의 `status` 분기 하나로 끝낸다. 제품이 늘어도 손댈 곳이 없다.

**대기자 등록은 `mailto:` 링크로 시작한다.** 폼 백엔드·DB·이메일 서비스 없이 출발하고, 문의가 실제로 오기 시작하면 무료 폼 엔드포인트로 교체한다(HTML `action` 한 줄).

---

## 기술 선택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프레임워크 | Astro | md 폴더를 타입 검증까지 해서 읽어주는 기능(Content Collections)이 내장이다. 로더를 직접 안 쓴다. |
| i18n | Astro 내장 i18n 라우팅 | 파일명 규약을 그대로 먹는다. 번역 라이브러리 불필요. |
| 스타일 | 순수 CSS + 커스텀 프로퍼티 | 제품 두 개가 이미 이 방식이다. 일관성 + 의존성 0. |
| 배포 | Cloudflare Pages | 정적 호스팅 무료. |

Next.js도 가능하지만 md 스캔과 스키마 검증을 직접 써야 해서 이 용도에는 코드가 더 나온다.

`Products/` 폴더는 지금 자리에 그대로 둔다 — 사이트 소스 안으로 옮기지 않는다. 제품 설명은 사이트보다 오래 살 문서다.

---

## 디자인 방향

디자인은 별도 스킬로 진행한다. 이 절은 그 작업의 제약 조건이다.

### 색: 이미 있는 의미를 재사용한다

Hestia가 정의한 온도 체계를 사이트 전체 규칙으로 승격한다.

- **시안** = 지금 살아 있는 것 (live, beta, 활성 상태)
- **호박** = 아직 붙잡아 둔 것 (prototype, 진행 중)
- **회색** = 아직 시작 안 한 것 (idea)

status 배지 색이 이 규칙을 따른다. 색을 새로 고르는 대신 제품이 이미 쓰고 있는 언어를 가져온다.

### 랜딩은 절제한다

Hestia 랜딩페이지에는 전면 캔버스 연출이 있다. 이 사이트는 그만큼 화려하게 가지 않는다. **제품 목록이 주인공**이고, 연출은 각 제품의 개별 페이지·데모의 몫으로 남긴다. 사이트가 제품보다 눈에 띄면 실패다.

### 필수

- 라이트/다크 (라이트 기본, 첫 방문은 시스템 설정 따름)
- 반응형 (카드 그리드 → 1열)
- 카드 하나가 담을 것: 이름, 태그라인, status 배지, 태그

---

## 작업 순서

1. md 파일 리네임 + 프론트매터 추가 (영어판은 `Apollo.en.md` 하나만 시범)
2. Astro 프로젝트 생성 + i18n 설정 + 컬렉션 스키마
3. 디자인 토큰 CSS → 카드 그리드
4. 상세 페이지 + status별 CTA
5. 다크/라이트, 반응형
6. Cloudflare Pages 배포

---

## 지금 넣지 않는 것

결제 시스템, 검색, 블로그, 애널리틱스, CMS, 태그 필터 UI.

제품이 5개를 넘거나 실제 방문자가 생긴 뒤에 붙인다. 제품이 2개인 사이트에 검색과 필터를 다는 것은 만드는 사람만 즐거운 일이다.
