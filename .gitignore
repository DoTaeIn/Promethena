# build output / deps (any subproject)
node_modules/
dist/
.astro/
.agents/
.claude/
.codex/
.impeccable/

# OS junk
.DS_Store
Thumbs.db

# env
.env
.env.*

# local-only tool state (not shared config)
.claude/settings.local.json
.impeccable/*.local.json
.impeccable/hook.cache.json
