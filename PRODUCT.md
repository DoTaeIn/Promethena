# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

Astro + 순수 CSS(커스텀 프로퍼티), Cloudflare Pages 정적 배포. PLAN.md에서 확정된 선택이며, 이유는 md 폴더를 스키마 검증까지 해서 읽는 Content Collections와 내장 i18n 라우팅이다. 서버·DB·월 고정비 없음.

## Users

두 부류가 같은 화면을 본다.

- **의뢰·채용 검토자** — 만든 사람의 실력을 확인하러 온다. 제품이 실제로 동작하는지, 무엇을 어디까지 만들었는지를 짧은 시간에 판단하려 한다.
- **제품을 살 만한 실사용자** — Apollo·Hestia 같은 도구가 자기 일에 쓸 만한지 보러 온다. `live` 제품은 구매, 그 전 단계는 대기자 등록으로 이어진다.

두 부류 모두 첫 화면에서 "이게 뭐고 지금 어느 단계인가"를 알아야 한다.

## Product Purpose

만든 소프트웨어 제품을 모아 보여주고 파는 정적 사이트. 판매와 포트폴리오를 한 사이트에서 겸한다.

성공 기준은 두 가지다.

1. **제품 추가의 마찰이 0.** `Products/{slug}.{lang}.md` 하나를 쓰면 사이트에 반영된다. 라우팅·목록·링크 코드를 손대는 일이 없어야 한다.
2. **실제 판매가 굴러간다.** `live` 제품의 구매, 그 이전 단계의 대기자 등록이 실제로 발생한다.

## Positioning

제품이 완성되기 전부터 정직하게 올린다. 대부분의 제품 사이트는 출시된 것만 보여주지만 이 사이트는 `idea → prototype → beta → live` 상태를 배지로 드러내고, 각 상태에 맞는 행동(없음 / 대기자 등록 / 데모 + 대기자 / 구매)만 요구한다. "완성된 것만 올린다"로 가면 사이트가 영원히 비어 있다는 판단에서 나온 규칙이다.

## Operating Context

- 제품 설명 원본은 `Products/` 폴더에 그대로 남는다. 사이트 소스 안으로 옮기지 않는다 — 제품 문서는 사이트보다 오래 산다.
- 현재 제품: **Apollo**(공유 세계관 소설 플랫폼, Python/FastAPI, 프로토타입), **Hestia**(팀 문서 협업 플랫폼, Next.js 15, 프론트엔드 프로토타입 — 백엔드 없음).
- 본문 형식은 제품마다 다르고 통일하지 않는다. 카드·필터에 필요한 값만 프론트매터로 뺀다.
- 대기자 등록은 `mailto:`로 시작하고, 문의가 실제로 오면 무료 폼 엔드포인트로 교체한다.

## Capabilities and Constraints

- 경로: `/`(ko 홈), `/p/{slug}`, `/en/`, `/en/p/{slug}`. 페이지 템플릿 2개(목록, 상세)로 전부 처리한다.
- 프론트매터 필수 필드: `name`, `tagline`, `status`, `tags`. 빠지면 **빌드 실패** — 조용히 깨진 카드가 배포되는 일을 막는다. 선택: `price`(`live`일 때만 의미), `demo`(없으면 데모 버튼 숨김).
- 한국어가 본문 기본, 제품명은 영문. 한쪽 언어 파일이 없으면 그 언어 목록에서 자동 제외된다 — 영어판은 천천히 채워도 사이트가 깨지지 않아야 한다.
- 라이트/다크 필수(라이트 기본, 첫 방문은 시스템 설정 따름), 반응형 필수(카드 그리드 → 1열).
- 카드가 담을 것: 이름, 태그라인, status 배지, 태그.
- 지금 넣지 않는 것: 결제 시스템, 검색, 블로그, 애널리틱스, CMS, 태그 필터 UI. 제품 5개 초과 또는 실제 방문자 발생 이후에 검토한다.
- 서버·DB 없음, 월 고정비 0원.

## Brand Commitments

- 이름 **Promethena** 확정.
- 로고 확정: `promethena-logo.png`, `promethena-logo-dark.png` (모두 투명 배경 PNG). 웹 자산은 `site/public/promethena-mark.png`, `site/public/promethena-mark-dark.png`이며, 헤더는 테마별 원본을 전환한다. PNG 파비콘·Apple 터치 아이콘은 밝은 로고 원본을 사용한다.
- 말투: 제품 md와 같은 톤(담백, 과장 없음, 미완성 상태를 숨기지 않음). 명시적으로 확정된 보이스 가이드는 아직 없다.
- **색 의미는 이미 확정** — Hestia가 정의한 온도 체계를 사이트 규칙으로 승격한다. 시안 = 지금 살아 있는 것(live, beta), 호박 = 아직 붙잡아 둔 것(prototype, 진행 중), 회색 = 아직 시작 안 한 것(idea).

## Evidence on Hand

- 실제 제품 문서 2건: `Products/Apollo.md`, `Products/HESTIA.md` (기능·기술 스택·구현 범위가 구체적으로 적혀 있음).
- Hestia는 만져볼 수 있는 프론트엔드 프로토타입 5화면이 존재한다.
- **없는 것: 고객, 매출, 후기, 사용자 수, 벤치마크, 프레스, 가격 확정.** 어떤 것도 지어내면 안 된다. `live` 제품이 아직 하나도 없다.

## Product Principles

1. **md 하나가 제품 하나다.** 사이트 코드를 고쳐야 제품이 늘어난다면 설계가 틀린 것이다.
2. **상태를 숨기지 않는다.** 프로토타입을 완성품처럼 보이게 만드는 모든 장치를 거부한다.
3. **제품 목록이 주인공.** 사이트가 제품보다 눈에 띄면 실패다. 연출은 개별 제품 페이지와 데모의 몫이다.
4. **색은 새로 고르지 않고 물려받는다.** 제품이 이미 쓰는 온도 체계를 사이트가 따른다.
5. **운영 비용 0을 깨는 기능은 지금 넣지 않는다.**

## Accessibility & Inclusion

제품 고유 요구사항으로 확정된 것은 없음. status 배지가 색 하나로만 의미를 전달하지 않도록 라벨 텍스트를 함께 두는 것은 위 규약에 이미 포함된다.
